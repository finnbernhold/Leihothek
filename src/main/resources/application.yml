spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory
    username: postgres
    password: example

  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  mail:
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  error:
    include-message: always
  port: 8080
